<%= form_with(model: sports_person, local: true) do |form| %>
  <% if sports_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sports_person.errors.count, "error") %> prohibited this sports_person from being saved:</h2>

      <ul>
      <% sports_person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :team %>
    <%= form.text_field :team %>
  </div>

  <div class="field">
    <%= form.label :avatar %>
    <%= form.file_field :avatar %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
